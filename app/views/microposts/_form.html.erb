<%= form_with(model: micropost, local: true) do |form| %>
  <% if micropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(micropost.errors.count, "error") %> prohibited this micropost from being saved:</h2>

      <ul>
      <% micropost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :micropost_description %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :micropost_number %>
  </div>

  <!-- <div class="field">
    <%= form.label :userss_id %>
    <%= form.text_field :userss_id, id: :micropost_userss_id %>
  </div> -->
  <div class="field">
  <%= form.label :userss_id %>
  <select name="micropost[userss_id]" id="micropost_userss_id">
    <% @useres=Userss::all().each do |userss|%>
    <option>
    <%=userss.id %>
    </option>
    <% end %>
  </select>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
