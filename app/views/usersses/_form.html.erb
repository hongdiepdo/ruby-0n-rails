<%= form_with(model: userss, local: true) do |form| %>
  <% if userss.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userss.errors.count, "error") %> prohibited this userss from being saved:</h2>

      <ul>
      <% userss.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :userss_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :userss_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
